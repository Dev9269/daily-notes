# Troubleshooting

## pytest breaks with DeprecationWarning

Use `-W ignore::DeprecationWarning`.

## CI never starts (action_required)

Maintainer must approve first run.

## 403 on API rerun

No admin permission; ask a maintainer.
